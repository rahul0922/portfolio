RewriteEngine On

# Single redirect: any www OR http → https://rahulfx3d.studio (avoids redirect chains)
RewriteCond %{HTTP_HOST} ^www\. [NC,OR]
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://rahulfx3d.studio/$1 [L,R=301]
